---
# tasks file for roles/centos/Prometheus

- name: Add Prometheus RPM Repository
  copy:
    src: prometheus.repo
    dest: /etc/yum.repos.d/

- name: Updating Cache
  yum:
    update_cache: yes

- name: Install Prometheus
  become: yes
  dnf:
    name:
      - prometheus2
      - node_exporter
