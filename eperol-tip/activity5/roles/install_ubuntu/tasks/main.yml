---
- name: Install Java
  apt:
    name: default-jre
    state: present
    update_cache: yes

- name: Install
  pip:
    name:
      - ansible
      - boto
      - python-openstackclient

# tasks file for roles/install_ubuntu
