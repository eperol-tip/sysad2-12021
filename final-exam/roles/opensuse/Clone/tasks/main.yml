---
- name: Install Git
  zypper:
    name: git
    state: latest

- name: Clone Prelim in opensuse
  git:
    repo: '{{ prelim_exam }}'
    dest: ./prelim-exam
    version: prelim-exam
# tasks file for roles/opensuse/Clone
